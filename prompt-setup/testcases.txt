I need to login test case and a logout test case for wk-all-in-one-framework location for https://opensource-demo.orangehrmlive.com/ with the provided user credentials in .env for selenium junit java, selenium cucumber bdd java, playwright junit java, playwright cucumber bdd java, wdio tdd, wdio bdd cucumber, cypress tdd, cypress bdd, playeright typrescript tdd test runner. java related pages, utils, constants and all go to the relavant tool location of the wk-all-in-one-framework\src\main\java and typescriptrelated pages, utils, constants and all go to the relavant tool location of the wk-all-in-one-framework\src\main\typescript.
Java tests or steps go to wk-all-in-one-framework\src\test\java location with different tool having different folders. Cypress and playwright typescript tests can go to a convenient place like wk-all-in-one-framework\tests or wk-all-in-one-framework\specs, you decide.
Also add the API tests with playwright and cypress for the https://jsonplaceholder.typicode.com/ referring to postman tests in java tdd junit, java bdd, typescript tdd for playwright and tdd and bdd for cypress for the 4 operations in the postman collection. 
sr-link-git-module-framework folder consists separate projects as in git modules, please add the same test cases here also.
Only difference here dell-maven-module-framework is most of the multiple autoamtion setups are java maven modules. Please set up the same test cases here too.
Please configure the test cases properly. Then we can execute them separately and see how the separate dashboards and management dashboards reflect them
Prepare a proper plan and hand over to composer 2.5 to implement.

Plan Questions:
1. Dashboard reflection after runs
A (recommended): Each tool writes a JUnit/JSON report; thin scripts call analytics-dashboard import (extend importers) so history lands in .analytics-data/{tool}/ and both project + management dashboards update.

2. Management dashboard sources
A: One source per project+tool (e.g. WK Selenium UI, WK Playwright API) with applicationType UI or API.
The source can be provided in the config file for management dshboard, from the projects itself local machine or in a jenkins machine, can copy the hystorical data from each project to the dashboard-for-management in a folder like ".analytics-data" or in sharepoint location or in s3 location or someplace like that for management.
(Our historical-analytics-dashboard library should provide the features for both project wise historical dashboard where the hystorical data will be within the project local machine or jenkins machine and the management dashboard features as specified. these two set ups can be configured from the config.ts file)

I can only see the playwright-java-api-tdd, selenium-java-tdd, playwright-java-api-tdd. I don't see the java selenium and playwright BDD test results, playwright typescript tdd test results or the tdd and bdd typescript cypress and wdio results. Please fix the issues.
Also "Historical Pass / Fail Trend & Response Times" chart is not utilizing the allocated space properly, please make it fit to the container to se the axis details and the chart details properly.
Also in the project wise dashboard "Execution Ledger" chart need to see the Application Type, Application and Project fields.
Also Frameworks Tracked tracked tile should be removed from the project wise dashboard and change it to Application Types
In the management dashboard remove the Framework filter. However, the connected filters should be synced only valid filters should display for selections.
In the project wise dashboard also there should be Application Type, Tool filters available and they should also be sync with linked values

In the Tools filter it should be cypress-bdd, cypress-tdd, newman, playwright-java-bdd, playwright-java-tdd, playwright-ts-tdd, selenium-java-bdd, selenium-java-tdd, wdio-java-bdd, wdio-java-tdd
Since we have the Application Type filter for UI, API, Mobile the combinations can be handled.
Test cases executed for apis except newman, there is only one test case execution. Execute all the 4 test cases just like in newman & if possible reflect the assertion details and execution results of the assertions from the execution results files.
Test cases for UI doesn't show the assertions & if possible reflect the assertion details and execution results of the assertions from the execution results files.
Configure the above changes from the current report changes needed
Also run the other 2 projects sr-link and dell since those two project dashboards also need to be tested and sync with management dashboard.
Once these are configured add a readme.md file in the root folder explaining how to run the separate projects and how to get the two dashboard types working step by step.
Also lets have four links for project wise 3 dashboards and one for management dashboard as github preview pages.

